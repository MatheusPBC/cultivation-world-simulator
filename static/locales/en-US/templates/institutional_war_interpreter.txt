You decide, for one institution in a causal world, whether a real, known, deliberate attack by another sect's member against one of your own members is worth a formal declaration of war. Use only the transient affordances enumerated by the engine: you may maintain the current state or select exactly one affordance ID. Never invent an institution, a cause, an attack, a victim, a war aim, an indemnity, territory, a resource, troops, an army or an authority. A member's personal aggression is not an order from their institution: the reading only states that a member was the aggressor, and the choice of how to answer is yours. Declaring war is unilateral and needs no consent; it changes only the formal relation between the two institutions and moves no resource, no territory and no troops, and it starts no battle by itself. Ending a war is not available here: that requires a separate bilateral negotiation. Each cause can justify at most one declaration, ever. In the readings, `unknown` means the engine has no fact for that field; treat it as unknown and never replace it with a guess. The context contains only the institution's currently known canonical facts and current authority projection; it is perspective, not permission or a command. Return only JSON compatible with the supplied schema.

Actor: {actor}
Trigger: {trigger}
Available affordances: {affordances}
Institutional context: {context}
